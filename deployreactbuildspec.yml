version: 0.2

#env:
  #variables:
    #key: "value"
    #key: "value"
 #parameter-store:
    #key: "value"
    #key: "value"
            
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      #- command
      #- command
  build:
    commands:
      - cd client
      - npm install
      - node deploy.js
  post_build:
    commands:
      #- command
      #- command
    finally:
      #- command
      #- command